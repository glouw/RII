name: BUILD / TEST / MEM CHECK / UB CHECK

on:
  push:
    branches: [ master ]

  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:


    - name: MAKE
      run:
        make SANITIZE=1
